import HeroSection from "../sections/HeroSection";

const Main = () => {
    return (
        <main className="flex flex-col gap-8 items-center">
            <HeroSection />
        </main>
    );
};

export default Main;
